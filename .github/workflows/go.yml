name: Go

on:
  push:
  pull_request:

env:
  docker_username: ${{ secrets.DOCKER_USERNAME }}
  docker_password: ${{ secrets.DOCKER_PASSWORD }}

jobs:
  Clone-down:
    name: Clone down repo
    runs-on: ubuntu-latest
    container: golang:latest
    steps:
      - uses: actions/checkout@v2
      - name: Upload Repo
        uses: actions/upload-artifact@v2
        with:
          name: code
          path: ./frontend

  DockerImage-frontend:
    runs-on: ubuntu-latest
    needs: [Build, Tests]
    steps:
      - name: Download artifact code
        uses: actions/download-artifact@v2
        with:
          name: code
          path: .
      - name: Login to DockerHub
        uses: docker/login-action@v1
        with:
          username: ${{ env.docker_username }}
          password: ${{ env.docker_password }}
      - name: Build and push
        uses: docker/build-push-action@v2
        with:
          push: true
          context: .
          tags: ${{ env.docker_username }}/simple-fortune-cookie-frontend:${{ github.sha }}

# test
  DockerImage-backend:
    runs-on: ubuntu-latest
    needs: [Build, Tests]
    steps:
      - name: Download artifact code
        uses: actions/download-artifact@v2
        with:
          name: code
          path: .
      - name: Login to DockerHub
        uses: docker/login-action@v1
        with:
          username: ${{ env.docker_username }}
          password: ${{ env.docker_password }}
      - name: Build and push
        uses: docker/build-push-action@v2
        with:
          push: true
          context: .
          tags: ${{ env.docker_username }}/simple-fortune-cookie-backend:${{ github.sha }}
  Tests:
    runs-on: ubuntu-latest
    needs: Clone-down
    container: gradle:6-jdk11
    steps:
      - name: Download code
        uses: actions/download-artifact@v2
        with:
          name: code
          path: ./frontend && ./backend
      - name: Build frontend
        run: |
          cd ${GITHUB_WORKSPACE}/frontend
          go build .
      - name: Run frontend
        run: |
          cd ${GITHUB_WORKSPACE}/frontend
          go run frontend &
      - name: Test Frontend
        run: |
          chmod +x ${GITHUB_WORKSPACE}/scripts/test_frontend.sh
          ${GITHUB_WORKSPACE}/scripts/test_frontend.sh

  Build:
    runs-on: ubuntu-latest
    needs: Clone-down
    steps:
      - uses: actions/checkout@v2

      - name: Set up Go
        uses: actions/setup-go@v2
        with:
          go-version: 1.17

      - name: Build
        run: go build
        working-directory: ./frontend

      - name: Test
        run: go test
        working-directory: ./frontend
